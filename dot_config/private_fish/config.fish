# To run commands only for interactive sessions, use:
if status is-interactive
    # Interactive commands can go here
end

# To run commands only for login shells, use:
if status is-login
    # Login commands can go here
end
